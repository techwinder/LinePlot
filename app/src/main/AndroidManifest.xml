<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="techwind.util.lineplot" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:versionCode ="1"
        android:versionName ="0.1"
        android:theme="@android:style/Theme.Material">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PrefsAxisActivity" />
        <activity android:name=".PrefsGraphActivity" />
        <activity android:name=".PrefsFileActivity" />
        <activity android:name=".PrefsVarActivity" />
        <activity android:name=".PrefsVariableSelActivity" />
        <activity android:name="techwind.util.file.FileDialogActivity" />
    </application>

</manifest>
